{
  "name": "mefai-predict-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ mefai-predict-subgraph",
    "create-local": "graph create --node http://localhost:8020/ mefai-predict-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ mefai-predict-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mefai-predict-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "matchstick-as": "0.6.0"
  }
}
